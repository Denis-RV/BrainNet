# # Java answers

status: В процессе
format: -
Tags: #java 
links: https://habr.com/ru/post/529214/
https://habr.com/ru/post/529214/

---
### Советы:
1. Необходимо придумать примеры в ответ на каждый вопрос.

---
### Личные вопросы:
- Расскажите о себе.
	- Можно добавить размышления о том, насколько важен диплом в наше время. В этом случае надо проанализировать обучение в вузах и убедительно рассказать работадателю, почему стоит выбирать меня, и не стоит смотреть на студентов, которые вероятно ничего не знают. Здесь стоит упоминуть то, что ко мне обращались студенты старших курсов из разных вузов (или проходящие курсы) за помощью с программированием, и их тесты были убогими (привести конкретные примеры). Рассказать, почему вузы мешают людям становится экспертами. 
	```
	Поступал в вуз я лишь с одной целью - с целью взять от туда максимум пользы для своей карьеры, но я уже тогда понимал, насколько неактуальна и мертва наша система образования и что это навряд ли мне что-то даст. Я поступил и теперь с уверенностью могу сказать, что эта система не только не приносит пользы - она, к тому же, ущербна и мешает человеку развиваться и становится компетентным. Меня заставляли тратить уйму своего времени на предметы, которые никак не связаны с моей специальностью, такие как физкультура (по которой нас заставляли писать огромные конспекты от руки), экология, культурология, физика и т.д. Спасибо, но мне хватило 11 лет изучения всего этого. И это не относится к общему развитию. Общее развитие - это умение себя презентовать, умение общаться, понимания рынка и бизнеса, умение обращаться с финансами, знание законов и различных кодексов, умение управлять командой - то есть все то, чем должен владеть образованный человек. В итоге получается, что образованный человек и человек с образованием - это два совершенно разных человека, у которых нет ничего общего.
	``` 
- Что именно ты изучал? Какие темы и на каких ресурсках? 
```
- Решил более 1000 различных задач из книг и курсов, прошел курсы от JavaRush, Udemy, Otus и десятки курсов на ютубе, прочел целиком книги по PostgreSQL, Head First, C++, Command Line Linux, Чистый Код, Грокаем Алгоритмы, Погружение в паттерны и сделал небольшую выжимку из книг Sprint 5 для профессионалов, Регулярные выражения, HTML and CSS (Head First), Филоссофия java (Брюс Эккель). Брал консультацию у профессиональных разработчиков пол года назад, чтобы подкоректировать свой курс.

- Имел опыт работы в администратировании серверов. Работал на сервере одной комерческой компании, занимающейся продажей газа ("Лентеплогаз"), и на сервере частной школы. Писал скрипты для автомасического бэкапа (дифференциального и ...), устанавливал сертификаты ssl, разворачивал VPN и синхронизировал некоторых 
```
- Расскажите о себе на английском. (5-10 предложений, где родился,что закончил, где работал, семейное положение)
```

```
- Расскажите о своем хобби на английском.
```

```
- Расскажите о своих сильных и слабых сторонах на английском.
```

```
- Расскажите, почему вы решили работать в нашей компании на английскомю
```

```
---
### Вопросы по ООП:
- Java - это ООП язык. Как вы это понимаете?
```
Объектно Ориентированное Программирование - это программирование, в основе которого лежат классы и объекты. Каждый объект является экзмепляром некого класса, а классы образют иерархию наследования.

ООП парадигма позволяет переложить объекты из нашего мира в наш код. Когда мы пишем программу, мы устанавливаем связи и взаимодействия между объектами подобно тому, как они взаимодействуют в реальном мире. 
```
- Преимущества и недостатки ООП?
```
Преимущества: Программы легко воспринимать, можно без труда понять чужой код, потому что все консепты имитирует то, что мы видим в реальной жизини. Код легко использовать повторно, благодаря наследованию. Также относительно легко работать над большими проектами.
Недостатки: Избыточность кода, особенно когда нам нужна маленькая программка. Это влияет и на память, и на скорость разработки и на понимание.
```
- Что такое инкапсуялция? 
```
- Инкапсуляция. 1) Позволяет закрывать наш объект для неправильного использования путем модификатора доступа. Если мы хотим добавить фото, обозначающее дату создания объекта, если мы не закроем это поле модификатором доступа, любой программист может использовать это поле как хочет и цель этого поля теряется. Таким образом мы защищаем наши объекты от неправильного доступа и оставляем их открытыми для расширения. 2)Это способность объектов скрывать часть своего состояния и поведения от других объектов, предоставляя внешнему миру только определённый интерфейс взаимодействия с собой.Например, вы можете инкапсулировать что-то внутри класса, сделав его приватным (private) и скрыв доступ к этому полю или методу для объектов других классов. Более открытый режим видимости protected сделает это полеьили метод доступным в подклассах
```
- Что такое наследование?
```
- Наследование. Один объект расширяет функциональность другово, добавляя собственные, уникальные характеристики. Допустим нам надо создать какую-нибудь гитару. Она может иметь разную форму, состоять из разного материала, по-разному стоить, но это должна быть гитара, а не труба, и при проектировании мы берем за гитару некую абстракцию (родительский класс), который имеет такие поля, как цена, материал, форма и имеет методы: сыграть такую-то ноту, настроить тональность.
```
- Что такое полиморфизм?
```
- Полиморфизм. Один интефрейс - множество реализаций. Позволяет использовать одно имя для задания общих для классов действий. Например интерфейс "Издать звук" можно будет использовать как для класса "Птица", так и для класса "Слон".
```
- Что такое абстракция?
``` 
- Абстракция данных - 1)выделение значимых характеристик объектов. На том же примере с гитарой мы выделили значемые характеристики, которые мы объявили в абстрактном классе. Это переменные и назавания методов. 2)Это модель некоего объекта или явления реального мира, откидывающая незначительные детали, не играющие существенной роли в данном контексте.
```
- Что такое модификатор доступа? Какие они есть и для чего используются? 
```Модификаторы доступа нужны для защиты данных, с их помощью мы и используем инкапсуляцию. Модификаторов всего четыре:
1. public - класс доступен в любом метсе для всех
2. private - доступ внутри класса
3. package(default) - внутри пакетов.
4. protected - доступ внутри пакетов и доступ для наследников.

```
- Когда нужно использовать интерфейс, а когда абстрактный класс? Чем они отличаются?
```
1. Интерфейс только описывает поведение, что должен уметь делать объект, но не описывает, как он должен что-то делать. Интерфейсы не хранят состояние объекта.

	Абстрактные классы могут содержать реализацию и хранить состояние объекта. 
	
	Интерфейсы тоже могут содержать реализацию по умолчанию, но это возможность была добавлена для обратной совместимости, чтобы не наршушить работу программ, использующих интерфейс.

2. Все поля, объявленные в интерфейсы являются public static final, в абстрактном классе могут быть любые переменные.

3. В Java можно наследоваться только от одного класса, а интефрейсов можно реализовывать сколько угодно.

4. Абстрактные классы создают связь между классами, близкими по функционалу и назначению. Интерфейсы могут реализовывать никак не связанные между собой классы.

5. Чтобы реализовать интерфейс используется ключевое слово implements, чтобу унаследоваться от абстрактного класса - extends.
```
- МОжно ли создать 2 мептода с одинаковым именем и разным возвращаемым типом?
```
Да, но у них должны быть разные наборы параметров. При различии только в возврщаемом типе будет ошибка.
```
- Расскажите о том, какие могут быть отношения между объектами.
	```
	Ассоциация — это когда один объект использует другой, либо зависит от него. Двустороння ассоциация между объектами вполнt допустима. 
	Композиция -  это отношение «часть-целое» между двумя объектами, когда один из них включает в себя другой. Особенность этого отношения заключается в том, что компонент может существовать только как часть контейнера. 
	Агрегация - это менее строгий вариант композиции, когда один объект просто имеет ссылку на другой объект. Здесь контейнер не управляет жизненным циклом компонента. Компонент может существовать отдельно от контейнера. 
	```
	Подробнее: [[Design Principles]], [[UML-Диаграммы]]
---

### Java SE:
#### Работа языка
- Как работает Java?
```
Любая программа представляет собой набор инструкций. Компьютер понимает только машинный код, который не понимает человек, и для решения этой проблемы был сделан компилятор, понимающий язык на джаве. Он преобразует код на джаве в байт-код (это сделано для кросплатформенности). Java Virtual Mashine (JVM) преобразует байт-код в машинный код и программа запускается.
Для корректной работы JVM необходима библиотека java-классов. JVM вметсе с библиотекой - это и есть JRE (Java Runtime Environment). Используя JRE вы можете запускать программы на java, но не сможете их создавть, для этого нужен JDK (Java Development Kit). JDK включает в себя JRE, компилятор, документацию и утилиты.
```
- Как устроена память в Java?
```
Существует Stack (ограничен 1 мб) и Heap(куча, ограничена размером оперативной памяти). Примитивные типы и ссылки на объекты хранятся в стеке. При создании объекта переменной присваевается адресс этого объекта, а сам объект помещается в кучу. Когда мы сравниваем два объекта чере знак сравнения, мы сравниваем их адреса в стеке, и значение true возвращается лишь в случае, когда две ссылки указывают на один объект. 
```
- Как сборщик мусора понимает, что нужно удалить объект? Если объекты ссылаются друг на друга, уничтожит ли их сборщик мусора?
```
Сборщик муссора удаляет объект, когда на него нет ссылок из стека.

Если объекты ссылаются друг на друга, но ни на один их этих объектов нет ссылок из стека, то они уничтожатся.
```
- Что такое раннее и позднее связывание и в чем разница?
``` 
Позднее связывание тесно взаимодействует с полиморфизмом, ведь полиморфизм позволяет нам давать конкретные объекты на этапе runtime. Мы можем строить наш код, опираясь на класс "Гитара", и реализация мы можем сделать с помощью присваивания значений на этапе runtime.
```
- Чем отличается объявление от инициализации?
```
Объявляем мы переменные, которые не имеют никаких знчений, и если это не примитивная переменная (объект), то эта переменная выступает в качестве ссылки на объект, которого пока нету. 
- Инициализация - это объявленияе переменной и присваивание ей значения или объекта, на который эта переменная будет ссылаться.
```
- Почему в java отказались от множественного наследования?
```
Отказались от множественного наследование, но сделали множественную реализацию интерфейсов. Со множественным наследованием связано много проблем, например смертоносный ромб смерти. TODO дописать.
```
- Чем отличается JVM, JRE и JDK? Зачем они нужны?
```
JVM (Java Virtuale Mashine) преобразует байт-код в машиный код. Для корректной работы JVM необходима библиотека java-классов. JVM вметсе с библиотекой - это и есть JRE (Java Runtime Environment). Используя JRE вы можете запускать программы на java, но не сможете их создавть, для этого нужен JDK (Java Development Kit). JDK включает в себя JRE, компилятор, документацию и утилиты.
```
- Где находиться компилятор Java?
```

```
- Что такое string pool?
```
string-pool — структура в памяти, хранящая массив всех строк-литералов программы.  
String.intern(), соответственно, вернет строку из пула, при наличии таковой.

Пул строк - это набор строк, который хранится в памяти java heap. Если строка создается как литерал, то ищется поле соответсвия и из пула либо вернется ссылка, либо создаться строка и вернется ссылка. Если использовать конструктор стринга, то не будет искаться такой строки в пуле, но если вызвать у такой строчки intern(), то будет обращение к пулу. Пример:
String s1 = "Cat";
String s2 = "Cat";
String s3 = new String("Cat");
String s4 = s3.intern();
//Две ссылки указывают на одну строку в пуле:
System.out.println(s1==s2); //true
//s3 не имеет отношения к пулу
System.out.println(s1==s3); //false
//s4 ссылается на строку s3 в пуле
System.out.println(s1==s4); //true
```
- Что означает native? 
``` 
Это означает, что реализация написана на языке C/C++
```

#### Исключения
- Что вы можете сказать об исключениях? Какие они бывают?
```
Исключения бывают Checked и Unchecked:

Uncheked - исключения времени выполнения, непроверяемые исключения. Это исключения, не видит компилятор и которые возникают во время работы программы. Например это выход за пределы массива, деление на ноль, nullPointerException. Данные ошибки возникают по вине программиста, когда он неправильно написал программу. В try-catch такие исключения оборачивать необязательно.

Checked - ислкючения, проверяемые компилятором. Пока они не будут обернуты в try-catch или проброшены, программа не скомпилируется. Если мы пробрасываем исключения через основной метод, например main, то программа упадет, если будет вызвано исключение.

Проверяемые и непроверяемые исключения наследуются от Exception, который в свою очередь наследуется от Throwable - самой верхушки в иерархие исключений.

Кроме Exception также есть класс Error. Это ошибки, связанные с системой, их кидает JVM в результате нехватки памяти (OutOfMemoryError), переполнения стэка (StackOverflowError) и т.д. С ними ничего делать не надо. 
```
- Назовите ключевые классы Exception. (Не путать с интерфесами?) 
```

```

#### Java классы и методы
- Назовите методы Object.
```
- finalize() - Этот метод позволяет принудительно заставить сборщик мусора избавиться от ненужных классов, на которые нет ссылок. Этот метод вызывается автоматически.
```
- Что такое статическая и динамическая структуры? 
```

```
- Какие различия между примитивными и сылочными переменными? 
```

```
- Что такое static? Наследуются ли они?
```

```
- Чем различаются статические поля и методы от нестатических? 
```

``` 
- Что значит immutable object и как его сделать, зачем он нужен?
```

```
- Чем отличается String, StringBuilder от StringBuffer? Зачем они нужны?
```
String является immutable, т.е. любое изменение строки приводит к созданию нового объекта.

StringBuilder, StringBuffer являются mutable, то есть они изменяемы и нам не нужно создавать много объектов. StringBuilder был добавлен в java5 и он во всем идентичен классу StringBuffer, за исключением того, что он не сонхронизирован, что делает его значительно быстрее, но его опасно использовать в многопоточной среде.
```
- Откуда взялся диапазон значений у типов? 
- Массив - это примитив или объект? 
- Расскажите о ключевом слове final. 

#### Ввод/вывод в Java
- В чем отличие от java.nio и java.io? 
- Что такое сериализация и десериализация?
```
Сериализация - процесс прфеобразования объекта в пото байтов.

Десериализация - процесс создания объекта из потока байтов.
```
- Какие условия должны быть выполнены, чтобы экземлпяр класса можно было сериализовать?
```
1. Реализация интерфейса Serializable или Externalizable
2. Все поля класса должны быть сериализуемы или помечены словом transient.
3. Все предки должны быть сериализуемы или содержать публичный конструктор без параметров.
```
---
- Что означает transient?
```
Оно запрещает сериализовать поле.
```
- Какие классы позволяют ускорить чтение/запись за счет использования буффера?
```
BufferedInputStream, BufferedOutputStream, BufferedReader и BufferedWriter.
```
#### Generics
- Что такое Generics?
```
Это параметризированные типы. С их помощью можно объявлять классы, интерфейсы и методы, где тип данных указан в виде параметра.
```
- Как и для чего используются ключевые слова extends и super в обобщенных типах?
```
Эти слова нужны для наложения ограничений на тип, передаваемый классу или методу.

Слово extends означает, что может быть использован сам класс или любой наследник этого класса.

Слово super означает, что может быть исопльзован сам класс, либо его родитель.
```
- Что такое wildcard (Маска)?
```
- это дженерик вида ?, который означате, что тип может быть чепм угодно. На них можно также накладывать ограничения.
```

---
#### Многопоточность
- Что такое многопоточность?
```
Многопоточность - это средство, которое позволяет выполняться разные задачи одновременно.

Многопточность на основе процессов дает возможность разным программам или процесскам работать параллельно

Многопоточность на основе потоков дает возможность выполнять одновременно несколько задач в рамках одной программы.
```
- Как в java можно создать потоки?
```
1. Реализовать интерфейс runnable (Создать класс, в котором будет реализован интерфейс Runnable и передать экземляр этого класса в конструктор класса Thread.) и переопределить метод run.

2. Унаследоваться от класса Thread и переопредеить мето run.

Первый способ лучше, т.к в java нет множественного наследования.
```
- Можно ли остановить, поставить на паузу запущенный поток? Почему нельзя останавливать поток методом stop?
```
Поток можно оставновить методом стоп, но это опасно делать, потому задания в потоки должны выполнятся как транзакции, их нельзя просто взять и преравать на середине выполнения, потому что это может приветси к проблемам. Например если поток загружает данные в базу данных и мы его остановим, то поток мгновенно остановиться и данные будут повреждены. Вместо stop() нужно вызывать метод interrupt(), который меняет значение переменной потока isInterrupted с false на true
```
- Что такое потоки-демоны?
```
Это потоки, которые завершают свою работу после завершения всех основных потоков. Поток можно сделать демоном с помощью метода setDeamon(true).
```
- чем синхронизированные метод отличается от несинхронизированного? 
```
Синхронизированные метод может быть вызван одновременно только одним потоком.
```
-  Что такое Stream (стримы)?  
```

```
- Что такое мониторы? Какую задачи они решают? (речь о потоках) 
```
**Монитор** (объект синхронизации) - хранит состояние "свободно" или "занято" для критической секции.

**Критическая секция** - участок кода, в котором одновременно может находиться только один поток.

Когда мы помечам у метода класса слово synchronized то в качестве монитора мы используем сам класс:
```
- Что такое параллельное программирование (parrals stream)? Как распараллелить стримы из большой коллекции? Сколько потоков будет создано? 
```

```
- Что такое атомарные типы данных (Atomic)?
```
Это оболочки примитивных типов данных, которые предоставляют методы, выполняемые за 1 операцию. Это позволяет решить проблемы гонки потоков.
```
- Какая имплементация находиться у паралельных стримов? 
```

```
- Что такое экзекутер-сервис? 
```
Он позволяет выполнять задачи, используя пул потоков, а также вовращать результат выполнения задачи в виде объекта Future.
Экзекутер-сервис используется, когда требуется выполнять много разных задач в отдельных потоках. Мы можем поручать экзекутеру-сервису задания, а он будет распределять их по потоком. Если все потоки будут заняты, экзекутер-сервис добавит задания в очередь и отдаст задание первому освободившемуся потоку.
```
- В чем отличе методов submit и execute у ExecutorService?
```
Execute не возвращает значений и принимает в качестве параметра объект типа Runnable. Sumbit возвращает объект типа Future, из которого можно получить значение с помощью метода get. Он принимает в качестве параметра Runnable или Callable.
```
- В чем отличие интерфейсов Runnable и Callable?
```
Реализуя Runnable надо переопределить метод run с возвращаемым типом void, а в интерфейсе Callable нужно переопределить метод call, который возвращает значение нужного типа.
```
- Какую проблему решает ключевое слово valatile? 
```
 Словом volatile необходимо помечать переменные, с которыми имеют дело два и более потоков. Оно запрещает помещать заначение в кэш. Дело в том, что процессор может поместить значение переменной в кэш, и когда поток поменяет значение этой переменной, то для другово потока значение не поменяется (т.к. в медленной памяти значение не поменяется). Это слово может не решить проблему гонки потоков, если например мы инкрементируем переменную.
```
- Что делает synchronized блок? 
```
Этот блок помечает любой объект как монитор, и внутри себя содержит критическую секцию, доступ к которой может иметь одновременно лишь один поток, который берет ключ у монитора.
```
- Что является монитором, если метод помечен synchronized?
```
объект, у которого вызывается данный метод (this)
```
- Зачем нужен класс Phaser? 
```

```
- Что такое ReentrantLock? 
```

```
- Чем поток отличается от процесса?
```

```

---

#### Stream API
- Что делает метод stream()?
```
Превращает коллекцию в поток данных. С этимп потоком можно работать в функциоанльном стиле и выполнять множество операций (сортировка, фильтрация, преобразование) очень компактно, не создавая множество копий коллекций.

Также, работая со стримами, можно легко распараллелить задачи, вызвав метод parallelStream. В обычном случае пришлось бы вручную разбивать коллекцию на несколько, выполнять операции и потом склеивать их.
```
- Чем отличаются терминальные операторы от промежуточных?
```
Терминальные операторы обрабатывают эелементы и завершают работу стрима, поэтому он может быть только один. Он запускает выполенинеие всех промежуточных операторов.

Промежуточные - обрабатывают поступающие элементы и возвращают стрим. Промежуточных операторов в цепочке обработки элементов может быть много.
```
- Что делает метод map?
```
Применяет операцию преобразования к каждому элементу потока. На выходе элементы могут иметь любой тип.
```
- Что такое функциональный интерфейс?
```
Это интерфейс с одним абстрактным методом. В нем могут также содержаться интерфейсы с реализацией по умолчанию. Эти интерфесы можно эффективно использовать в лямда-выражениями.
```
- Чем отличается терминальный оператор от промежуточного?
```
Терминальный оператор является завершающим - он обрабатывает элементы и завершает работу стрима, поэтому терминальный оператор в цепочке может быть только один. Он же запускает работу всех промежуточных операторов.

Промежуточные - обрабатывают поступающие элементы и возвращают stream. Им может быть много.
```
- Приведите примеры терминальных и промежуточных операторов
```
Терминальные - collect, count, max, min, forEach (все, которые возвращают не поток данных)Промежуточные - все, которые возвращают поток данных - filter, map, limit и другие.
```
- Что делает метод map()?
```
Применяет операцию преобразования к каждому элементу потока, на выходе элементы могут иметь любой тип.
```
- Что такое Optional и какое он имеет отношение к стримам?
```
Объекты Optinal является конфтейнерами, в которых может храниться объект или не хранится ничего. Сам объект из контейнера мы можем получить через метод get, перед этим выполнив проверку через метод isPrsent или используя функиональный стиль - через метод ifPresent.

Терминальные операторы, которые возврщаают элемент из потока (findFirst, min, max) возвращают Optinal, т.к. нет гарантий, что в потоке вообще есть данные.
```
- Как работает метод filter?
```
filter принимает в качестве параметра Predicate - функцию, которая возвращает тип Boolean - оставляем элемент в потоке или нет. Каждый элемент потока передается в эту функцию, если она возврщааюет true, то эелемент остается, если false - удаляется.
```
- Как происходит сортировка в sorted?
```
Сортируются лишь те классы, которые реализуют интерфейс Comporable. Иначе нам нужно в качетсве параметра передать Comporator и реализовать его.
```

### Collection Framework:
- Что делают методы equals() и hashCode()? 
```
Метод equals по умолчанию делает то же, что и знак равенства: он сравнивает ссылки двух объектов. Чтобы этот метод сравнивал объекты по значению полей, необхоидоме этот метод переопределить.

Метод hashCode служит для целочисленного представления объекта. Для хеш-кода должно выполнятся правило: Если объекты равны по equals(), то и хеш-коды у них должны быть равны, но не наоборот.

Если объекты не равны по результатам выполнения метода equals, тогда их hashcode могут быть как одинаковыми, так и разными. Однако для повышения производительности лучше, чтобы разные обхъекты возвращали разные коды.

У двух разных объектов может совпасть хеш-код, т.к. этот хеш-код типа int и принимает не больше 2 млрд значений, а объектов может быть создан больше 2 млрд, это называется коллизией.

У одинакоыйх строк одинаковый хеш-код

```
- Почему в массив нельзя добавить элемент? 
```

```
- Какие ограничения у дженериков?
```
1. В качестве параметра обобщенного типа можно использовать только ссылочный тип.
2. Нельзя создать объект обобщенного типа.
3. Нельзя создать массив объектов обобщенного типа.
```
- Чем отличаюстся коллекции ArrayList и LinkedList?
```
ArrayList - это динамический массив, который увеличивается по мере добавления элементов. LinkedList - это связный список, в котором каждый элемент имеет ссылку на предыдущий элемент и на следующий.
Добавление и удаление элементов в середине LinkedList прооисходит с алгоритмической сложностью O(1), когда в ArrayList это делается за O(n). Поиск и удаление по индексу в ArrayList делается за O(1), а в LinkedList за O(n).
```
- Назовите самый эффективный способ копирования массива? (class Arrays) Чем будет отличаться копирование массива просто циклом и с помощью класса Arrays? 
```
System.arrraycopy(...)?
```
- Как устроена коллекция HashSet? TODO <?В тексте ошибка. На самом деле коллекция HashSet создается коллекция HashMap?>
```
В основе HasSet лежит HashMap,, которая в свою очередь основана на хеш-таблице. Когда мы добавляем значение в HashSet, в HasmMap кладется элемент с нашим объектом в качестве ключа и с экземляром класса Object в качестве значения. В обычных таблицах элементы идут подряд, без пропусков, а в хеш-таблице элементы могут лежать в разных ячейках, и распределяться они будут на основании хеш-кода. В HashMap изначально находится массив из 16 ячеек, и когда мы добавляем объект, вычисляется остаток деления хеш-кода этого объекта на число 16 (изначально), и на основе этого остатка определяесятя место в массиве. В сами ячеки массива мы помещаем объект класса Entry, который содрежит три поля - ключ нашего объекта, значение обхъекта(которое в HashSet равно экземляру класса Object) и сслыка на следующий объект Entry, который будет добавлен в случае, если нам поподется объект с таким же хеш-кодом. Это называестя коллизией. Ячейки в массивах работают как связный список, но для оптимизации также есть специально поле с плавающей точкой, в котором указан коэфицент 0.75. Это означает, что если массив будет заполнен на 75%, то массив увеличиватся в два раза. Еще у этой коллекции есть одна особенность: если в одной из ячеек произойдет 8 раз коллизия, то есть будет список из 8 связанных элементов, то будет использована древовидная структура, вметсо структуры связного списка. Исходя из этого можно утверждать, что сложность операций в этой коллекции O(log(N)).
```
- Как устроена коллекция HashMap?
```
Устроена также, как и HashSet, но в ней объект Entry хранит также и значения ключа. В основе лежит хеш-таблица (массив из 16 элементов), при добавлении нового объекта на основе уго хэш-кода вычисляется позиция в хэш-таблице. Если позиция не пустая, то с помощью метода equals проверяетя, не равен ли его ключ тому, что мы вставляем, и если он равен, то мы перезаписываем его значение, ели нет, то происходит коллизия, которая разрешается созданием связного списеа в той ячейке, в которой произошла коллизия. Еще у этой коллекции есть одна особенность: если в одной из ячеек произойдет 8 раз коллизия, то есть будет список из 8 связанных элементов, то будет использована древовидная структура, вметсо структуры связного списка. Исходя из этого можно утверждать, что сложность операций в этой коллекции O(log(N)).
```
- Алгоритмическая сложность операция в HashMap?
```
Лучший случай (все элементы распределены равномерно) - сложность всех операций O(1).
В худшем случае, когда происходит много коллизий и 
лементы в массиве образуют связные списки - сложность O(n) до 8 версии, и O(log(n)) начинаая с 8 версии. Это обусловлено тем, что если в коллекции образуется связный список более чем из 8 элементов, то этот список переделываестя в древовидную структуру.
```
- Как получить значения или ключи в HashMap?
```
С помощью метда keySet() получаем ключи, а с помощью values() - значения.
```
- Как HashMap связана с SetMap? 
```

```
- Что такое LinkedHashMap? 
```

```
- В каких случаях вы будете использовать коллекции ArrayList, LinkedList, HashSet, TreeSet, HashMap, LinkedHashSet, LinkedHashMap, ?
```
ArrayList - если нам не нужно добавлять и удалять элементы из сереины, но нужно добавлять много элементов и находить их по индексу.

LinkedList - если нам нужно часто добавлять и удалять элементы в середине и в начале, но не нужно искать и удалять их по индексу. Также он нужен, есил размер коллекции будет сильно уменьшаться.

HashSet - когда не важен порядок, чтобы в коллекции не было дубликатов.
HashMap - когда не важен порядок, когда необходимо отсутсвие дубликатов и нужно хранить объекты в виде ключ-значение.

ThreeSet - когда нам важна сортировка и ваожно отсутсвие дубликатов.
TreeMap - когда нам нужно коллекцию из отсортированных ключей, содержащих значения.

LinkedHashSet - когда нам важен порядок, в котором мы добавляем элементы. Работает как и TreeSet.
LinkedHashMap - если важен порядок добавления элементов.
```
- Чем отличаются интерфейсы Set и List?
```
Коллекции Set не могут хранить одинаковых элементов, в отличе от List. Интерфейс List также позволяет нам обращаться к элементам по индексу и удалять их по индексу, а Set это не повзоляет.
```
- Что представляет собой интерфейс Map?
```
Коллекции типа map хрянят объекты парами ключ-значение. 
Основные методы: put() (вставка), remove(), keySet(), values(), size(). Этот интерфейс не находистя в иерархии Collection Framework и ее нельзя перебирать в цикле foreach. Это сделано, так как из-за своей реализации Map не может реализовать методы из интерфейса Collection.
```
- Какие сходства и различия у HashSet и у TreeSet?
```
Сходства:
- реализуют интерфейс Set
- не хранят повторяющихся элементов

Различия:
- TreeSet хранит объекты в отсортированном виде
- HashSet может хранить обхекты любых классов, а TreeSet только тех, которые реализуют интрерфейс Comparable, либо любых классов при условии, что в качестве параметра в конструктор был передан Comporator.
- Адгоритмическая сложность операция вставки, удаления и поиска элемента: HashSet - O(1), TreeSet - O(log(n)).
```
- Что такое коллизия hash-кодов с точки зрения HashSet'a? 
```
Коллизия - это когда мы встречаем объект с совпадающием хеш-кодом, но в случае с нашими коллекциями хеш-коды совпаюат часто, т.к. мы вычисляем остаток от деления хеш-кода объекта на некоторое число.
```
- Какая будет сложность к доступу к элементу по ключу в общем и худшем случае при использованнии HashSet? 
```
Лучший случай (все элементы распределены равномерно) - сложность всех операций O(1).
В худшем случае, когда происходит много коллизий и 
лементы в массиве образуют связные списки - сложность O(n) до 8 версии, и O(log(n)) начинаая с 8 версии. Это обусловлено тем, что если в коллекции образуется связный список более чем из 8 элементов, то этот список переделываестя в древовидную структуру.
```
- Как устроен [[class TreeSet]]?
```
Коллекция строится в виде дерева и значения внутри этой коллекции сохранятюся в отсортированном виде. Предоставляет возможность управлять порядком элементов в коллекции при помощи объекта `Comparator`. Использовать можно только с классами, реализующими интерфес Comparable. Алгоритмическая сложность всех операций - O(log(n))
```
- Расскажите об иерархии классов Collection Framework.
```
На верхушке иерархии стоит интерфейс iterable, который позволяет нам перебирать коллекции с помощью foreach. Чуть ниже стоит интерфейс Collection, который содержит основные методы для коллекций. Далее идут интерфейсы List (для ArrayList и LinkedList), и Set (HashSet и TreeSet).
Отледльно от этой иерархии стоит интерфейс Map с реализациями HashMap, TreeMap, LinkedHashMap.
```
- При какой ситуации в коллекции HashMap может потеряться элемент?
```
Если в качестве ключа используется изменяемы объект и после того, как он был добавлен в коллекцию, у него изменили какое-то поле, от которого зависит хэш-код.
```
- Расскажите об интерфейсе Queue.
```
Данный интерфейс содержит методы для добавления элемента в конец очереди, для возврщаение первого элемента в очереди (или null), и метод для возвращения и удаления первого элемента из очереди (в случае пустой коллекции будет исключение.)
```
---


### Тестировнаие, докер, сервисы
- Какие инструменты для нагрузочного тестирования вы занаете?
```

```
- Чем докер отличается от виртуалки? Что лежит в его основе? Как работает Docker?
```

```
- Чем контейнер сервлетов отличается от сервера приложений?
```

```
---
### Шаблоны проектировнаия
- Что такое SOLID?
```

```
- Каких виды паттернов вы знаете?
```

```
- Какие паттерны знаете, расскажите про такие-то.
```

```
- Какие паттерны применяли на практики, и где они встерчаются?
```

```
- Чем декоратор отличается от прокси?
```

```
- Чем абстрактная фабрика отличается от обычной?
```

```
- Что такое coupling и cohesion? Что из них должно быть сильным или слабым?
```

```
- Что такое куб масштабирования?
```

```
- Какие вы знаете паттерын микросервисов?
```

```
- Расскажите о CAP-теореме. 
```

```
- Чем сервисная архитектура отличается от микросервисной?
```

```
- Какие ограничения есть у микросервисов? Назовите достоинства и недостатки микросервисной архитектуры.
```

```
- Чем композиция отличается от агрегации?
```

```
- Какие бывают тестовые объекты(заглушки)? Чем стаб отличаетс от мока?
```

```

---
### Java EE


---
### Spring
- Какие есть scope в Spring? Какой по умолчанию? Чем singleton отличается от prototype? Можно ли сделать свой scope и как?
- 'Как заинжектить prototype в singleton?
- Часто спрашивают о циклических зависимостях бинов в Spring. Проблема ли это или что получим в результате? Если проблема, то как её решить?
- Бывают вопросы про жизненный цикл бина, этапы инициализации контекста, про устройство спринга внутри, про DI и как он работает
- Расскажите про прокси и про @Transactional. Как работает и зачем? Какие могут быть проблемы? Можно ли навесить @Transactional на приватный метод? А если вызывать метод с @Transactional внутри другого метода с @Transactional одного класса - будет работать?
- Где у обычного (НЕ Boot) Spring-приложения main-класс?
- Как работает Spring Boot и его стартеры?
- Как выполняется http-запрос в Spring?
- Для чего нужен Component Scan?
- Как вы добавите Component Scan в Spring Boot?
- В чём отличие между @Component и @ComponentScan?
* Для чего используется аннотация @Bean?
* В чём разница между @Bean и @Component?
* В чём разница между @Component, @Service и @Repository аннотациями?
* Можем ли мы использовать @Component вместо @Service для бизнес логики?
* В чем различие между web.xml и the Spring Context - servlet.xml?
* Что предпочитаете использовать для конфигурации Spring - xml или аннотирование?
* Можем ли мы применить @Autowired с не сеттерами и не конструкторами методами?
* В чем разница между Сквозной Функциональностью (Cross Cutting Concerns) и АОП (аспектно оринтированное программирование)?
* В чем разница между IOC (Inversion of Control) и Application Context?

https://habr.com/ru/post/350682/

---
### Hibernate
* Какие есть кэши в Hibernate и какие работают по умолчанию?
```

```
* Чем отличается Lazy от Eager в Hibernate?
```

```
* Что такое 'проблема N+1 запроса' при использовании Hibernate? Когда возникает? Как решить? Как обнаружить?
```

```
* Как описать составной ключ при использовании Hibernate?
```

```
* Как можно отобразить наследование на БД с помощью (Hibernate)?
```

```
* Какие языки понимает Hibernate (как делать запросы)?  
```

```
* Что такое JPA?
```

```
* В чем отличие JPA от Hibernate?
```

```
* Что такое Enity?
```

```
https://habr.com/ru/post/265061/

---
### Rest-сервисы
- Что такое REST? 
```

```
- Какое API является RESTful?
```

```
- Чем REST отличается от SOAP?
```

```

---
### Data base, SQL:
- Что тафкое БД?
```
- совокупность самостоятельных данных любого типа, систематизированных таким образом, чтобы эти данные могли быть найдены и обработаны с помощью электроной вычислительной машины.
```
- Что такое СУБД?
```
- спкциальное программное обеспечение, которое управляет созданием и исполльзованием баз данных с помощью ЭВМ. Благодаря систематизации данных, хранимых в базе, СУБД может автоматически обарабатывать и находить данные по запросу.
```
- Что такое индексы в базе данных? 
```

```
- Как создать запрос в SQL? (выбрать людей на определенных условиях) 
```

```
- Какаю задачу решает join (в SQL)? 
```

```
- Как связать приложение и базы данных? 
```

```
- С помощью какой сущности выполняется запрос данных в базе данных? 
```

```
- Что такое критерии API? 
```

```
- Опишите жизненный цикл приложения на spring (что происходить, когда приложение стартует, выполняется запрос и приложение получает ответ?). 
```

```
- Что такое bin (в контесте рабаты со спрингом)? 
```

```
* В чем разница между where и having в SQL?
```

```
* ACID - расшифровать и немного раскрыть
```

```
* Уровни изолированности транзакции. Какую проблему решает каждый уровень? Какой по умолчанию в большинстве реляционных БД? Приходилось ли изменять уровень изолированности транзакции и для чего?
```

```
* Какие бывают типы индексов в БД и как они устроены под капотом? Что такое индекс вообще?
```

```
* В чем разница между оптимистической и пессимистической блокировками?
```

```